PREFIX dbo: <http://dbpedia.org/ontology/>
PREFIX dbr: <http://dbpedia.org/resource/>
PREFIX dbp: <http://dbpedia.org/property/>
PREFIX foaf: <http://xmlns.com/foaf/0.1/>

CONSTRUCT {
  ?player a dbo:Athlete ;
          foaf:name ?name ;
          dbo:birthDate ?birthDate ;
          dbo:nationality ?nationality ;
          dbo:position ?position ;
          dbo:height ?height ;
          dbo:number ?number ;
          dbo:team dbr:FC_Barcelona .
}
WHERE {
  {
    SELECT DISTINCT ?player ?name ?birthDate ?nationality ?position ?height ?number
    WHERE {
      dbr:2024–25_FC_Barcelona_season dbp:p ?player .
      OPTIONAL { ?player foaf:name ?name . }
      OPTIONAL { ?player dbo:birthDate ?birthDate . }
      OPTIONAL { ?player dbo:nationality ?nationality . }
      OPTIONAL { ?player dbo:position ?position . }
      OPTIONAL { ?player dbo:height ?height . }
      OPTIONAL { ?player dbo:number ?number . }
    }
    LIMIT 200
  }
}